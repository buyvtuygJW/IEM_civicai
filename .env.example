# docker-compose automatically reads a file named ".env" in this same folder
# and substitutes these into docker-compose.yml. Copy this file to ".env" and
# fill in whatever you need — everything below has a safe default for a demo,
# so an empty .env (or no .env at all) still works.

# ── Bring your own AI key (recommended) ──────────────────────────────────
# Set ONE of these to enable AI-powered classification, voice parsing, and
# Welfare Copilot chat. Leave all three blank to run entirely offline on
# rule-based logic. Priority if more than one is set: Anthropic, then OpenAI,
# then Gemini.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Google Gemini. Has a genuinely free tier -- grab a key at
# https://aistudio.google.com/apikey, no credit card required. Recommended if
# you just want a free prototype key. Also accepts GOOGLE_API_KEY.
GEMINI_API_KEY=

# Optional. Override the model. Defaults: claude-sonnet-5 / gpt-4o-mini /
# gemini-flash-latest.
AI_MODEL=

# Secret used to sign login tokens (JWTs). Change this before deploying
# anywhere real — the default is only safe for local demos.
JWT_SECRET=dev-secret-change-me-in-production

# Shared code required to register a government account. Change this before
# a real deployment — ideally replace the whole flow with SSO/admin approval.
GOV_REGISTRATION_CODE=CIVIC-GOV-2026

# Defaults to local SQLite. Point at Postgres when you outgrow it, e.g.:
#   DATABASE_URL=postgresql://user:password@host:5432/indicivicai
DATABASE_URL=sqlite:///./indicivicai.db
